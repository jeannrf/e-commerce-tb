---
import {
  Instagram,
  Facebook,
  Mail,
  MapPin,
  Phone,
  ShoppingBag,
} from "lucide-astro";
---

<footer class="relative bg-primary text-white overflow-hidden">
  <!-- Decorative background overlay -->
  <div class="absolute inset-0 opacity-5 pointer-events-none">
    <div
      class="absolute top-0 left-0 w-full h-full bg-[radial-gradient(circle_at_20%_30%,#C9A227_0%,transparent_50%)]"
    >
    </div>
  </div>

  <!-- Main Footer Content -->
  <div
    class="relative max-w-7xl mx-auto px-6 sm:px-10 lg:px-16 xl:px-20 py-16 lg:py-20"
  >
    <div
      class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-12 gap-12 lg:gap-16"
    >
      <!-- Brand Section -->
      <div
        class="lg:col-span-6 flex flex-col items-center md:items-start text-center md:text-left"
      >
        <a href="/" class="flex items-center gap-2 group mb-6">
          <div
            class="w-12 h-12 bg-white rounded-xl flex items-center justify-center group-hover:scale-110 transition-transform shadow-lg shadow-white/10"
          >
            <ShoppingBag class="w-6 h-6 text-primary" />
          </div>
          <span class="text-3xl font-extrabold tracking-tight">
            TRIBUNA<span class="text-accent-gold">SVR</span>
          </span>
        </a>
        <p class="text-white/70 max-w-md mb-8 text-lg leading-relaxed">
          La tienda favorita para el hincha blanquiazul. Vestimos la pasión, la
          historia y el amor por Alianza Lima.
        </p>

        <!-- Social Links -->
        <div class="flex gap-4">
          {
            [
              {
                Icon: Instagram,
                href: "https://instagram.com/tribunasvr",
                label: "Instagram",
              },
              {
                Icon: Facebook,
                href: "https://facebook.com/tribunasvr",
                label: "Facebook",
              },
              {
                Icon: null,
                href: "https://tiktok.com/@tribunasvr",
                label: "TikTok",
                svg: "M19.59 6.69a4.83 4.83 0 01-3.77-4.25V2h-3.45v13.67a2.89 2.89 0 01-5.2 1.74 2.89 2.89 0 012.31-4.64 2.93 2.93 0 01.88.13V9.4a6.84 6.84 0 00-1-.05A6.33 6.33 0 005 20.1a6.34 6.34 0 0010.86-4.43v-7a8.16 8.16 0 004.77 1.52v-3.4a4.85 4.85 0 01-1-.1z",
              },
            ].map((social) => (
              <a
                href={social.href}
                target="_blank"
                rel="noopener noreferrer"
                class="w-11 h-11 bg-white/5 border border-white/10 rounded-xl flex items-center justify-center hover:bg-accent-gold hover:text-primary hover:border-accent-gold transition-all duration-300"
                aria-label={social.label}
              >
                {social.Icon ? (
                  <social.Icon class="w-5 h-5" />
                ) : (
                  <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
                    <path d={social.svg} />
                  </svg>
                )}
              </a>
            ))
          }
        </div>
      </div>

      <!-- Links & Contact Container (2 columns on mobile) -->
      <div class="lg:col-span-6 grid grid-cols-2 md:grid-cols-2 gap-8">
        <!-- Quick Links -->
        <div>
          <h3 class="font-bold text-xl mb-6 text-accent-gold">Enlaces</h3>
          <ul class="space-y-4">
            {
              [
                { label: "Novedades 2026", href: "#novedades" },
                { label: "Colección Histórica", href: "#historica" },
                { label: "Accesorios", href: "#accesorios" },
                { label: "Sobre Nosotros", href: "#nosotros" },
              ].map((link) => (
                <li>
                  <a
                    href={link.href}
                    class="text-white/60 hover:text-white hover:translate-x-1 inline-block transition-all"
                  >
                    {link.label}
                  </a>
                </li>
              ))
            }
          </ul>
        </div>

        <!-- Contact -->
        <div>
          <h3 class="font-bold text-xl mb-6 text-accent-gold">Contacto</h3>
          <ul class="space-y-4">
            <li>
              <a
                href="https://wa.me/51982089087"
                target="_blank"
                class="flex flex-col gap-1 text-white/60 hover:text-white transition-colors"
              >
                <span
                  class="text-xs uppercase tracking-widest text-white/40 font-semibold"
                  >WhatsApp</span
                >
                <div class="flex items-center gap-2">
                  <Phone class="w-4 h-4 text-accent-gold" />
                  <span class="font-medium">+51 982 089 087</span>
                </div>
              </a>
            </li>
            <li class="flex flex-col gap-1 text-white/60">
              <span
                class="text-xs uppercase tracking-widest text-white/40 font-semibold"
                >Ubicación</span
              >
              <div class="flex items-center gap-2">
                <MapPin class="w-4 h-4 text-accent-gold" />
                <span class="font-medium">Lima, Perú</span>
              </div>
            </li>
          </ul>
        </div>
      </div>
    </div>
  </div>

  <!-- Bottom Bar -->
  <div class="border-t border-white/5 bg-primary-dark/30">
    <div class="max-w-7xl mx-auto px-6 py-8">
      <div class="flex flex-col md:flex-row items-center justify-between gap-6">
        <p class="text-white/40 text-sm text-center md:text-left">
          © {new Date().getFullYear()}
          <span class="text-white/60 font-semibold">Tribuna SVR</span>. Todos
          los derechos reservados.
        </p>
        <div class="flex items-center gap-2 text-white/40 text-sm italic">
          Hecho con <span class="animate-pulse text-red-500">❤️</span> para la hinchada
          blanquiazul
        </div>
      </div>
    </div>
  </div>
</footer>
